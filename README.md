# CHEST CANCER CLASSIFICATION USING MLFLOW + DVC

## Project Log

### 📅 17th Feb, 2026

### Step 1: Clone GitHub Repository

Commands used:

```
cd "your-folder-path"
git clone your-github-repo-link
```

### Step 2: Initial Project Setup

* Created `template.py`
* Added required folders and files

### Step 3: Push Code to GitHub

Commands used:

```
cd "your-folder-path"
git add .
git commit -m "initial project setup"
git push origin main
```

### Step 4: Created Conda Environment

* Created virtual environment for project dependencies
* Activated environment
* Installed required libraries (to be added)
* Pushed changes to GitHub
